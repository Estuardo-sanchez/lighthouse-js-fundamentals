const isCitizen = true;
const age = 26;

if (isCitizen && age > 18) {
  console.log("You are eliggible to vote.");
}

// line

if (temperature < -40 || temperature > 40) {
  console.log("Maybe going outside isn't such a great idea…");
}

// line

if (!raining) {
  console.log("Leave your umbrella at home!");
}